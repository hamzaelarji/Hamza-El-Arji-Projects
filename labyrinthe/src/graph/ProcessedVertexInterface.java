package graph;

public interface ProcessedVertexInterface {
	 void addVertex (VertexInterface vertex);                    // ajouter un sommet à processedvertex
	 boolean isVertexProcessed (VertexInterface vertex);         // vérifie si le sommet est dans processedvertex
}

